请基于Vue3 + TypeScript + pnpm + pinia + vue-router 构建一个**水墨武侠风格**的帮会展示网站，核心要求如下：
1.  风格定位：传统中式水墨风，以黑白灰为主色调，点缀赭石、靛蓝等古风色彩；页面带有宣纸纹理、毛笔笔触边框、水墨晕染过渡效果，避免现代简约/炫酷风格。
2.  核心功能：展示帮会60名成员，支持「分页展示」（每页10-15人）、「按职位筛选」（帮主、副帮主、长老、堂主、普通帮众）、「按职业筛选」、「按战力搜索/排序」、「成员卡片点击跳转/弹窗进入个人信息详情页」。
3.  职业设定（8大职业，需完整融入数据与展示）：
    -  神相：远程输出职业，机动性强
    -  碎梦：高机动性近战输出职业，输出能力强，用刀，造型帅气
    -  铁衣：防御力高，适合前排抗伤害
    -  血河：近战输出职业，防御和机动性中等
    -  素问：辅助职业，提供治疗和支持
    -  九灵：可以召唤宠物作战，输出能力可观
    -  龙吟：小型PVP强势职业，具备多种战斗形态
    -  玄机：高机动性远程职业，输出强
4.  页面结构：
    -  头部：帮会名称（毛笔字体）、帮会LOGO（水墨风格）、导航栏（首页、成员列表、帮会战绩、入帮申请），导航栏hover时有水墨划过效果。
    -  首页：核心区域为**帮会成员卡片全屏展示（或网格展示）**，无多余复杂模块，卡片直接承载成员核心信息，点击卡片即可进入个人信息详情页。
    -  成员卡片（首页/列表页通用）：「卷轴式」或「宣纸卡片式」，必须包含：水墨风格头像（可留白写意）、成员昵称（古风字体）、职业（8大职业之一）、职位、战力值，简洁直观。
    -  个人信息详情页：打开时以「卷轴展开/水墨晕染」方式呈现，包含完整信息：水墨头像（放大）、昵称、职位、职业（附带职业特性描述，如「碎梦 - 高机动性近战输出，用刀」）、战力值、门派、入帮时间、武侠风简介（1-2句），底部带有「返回列表」水墨印章按钮。
    -  底部：帮会联系方式、江湖公告、版权信息（水墨落款样式）。
5.  技术要求：
    -  采用Vue3 Setup语法糖开发，遵循TypeScript类型规范，定义清晰的成员数据接口（Interface），必须包含「职业」字段。
    -  状态管理：使用Pinia存储帮会成员数据、筛选条件、分页信息，确保数据全局共享且类型安全。
    -  路由：使用Vue Router配置首页、成员列表页、个人信息详情页、帮会战绩页，详情页通过「成员id」动态匹配路由（如/gang/member/:id），路由切换时添加水墨淡入淡出过渡动画。
    -  样式：使用SCSS/SASS预处理样式，封装水墨风格通用样式（如卡片、按钮、边框），支持响应式适配（PC端优先，兼顾平板端），避免使用第三方UI库（如需引用请自定义水墨主题覆盖原样式）。
    -  数据：模拟60条帮会成员数据，数据结构完整，包含id、nickname、position、profession、combatPower、faction、avatar、intro、joinTime等字段，确保类型匹配，职业分布均匀合理。
6.  交互优化：
    -  成员卡片hover时：出现水墨描边高亮、轻微上浮效果，光标变为毛笔样式，卡片底部浮现「查看详情」毛笔小字提示。
    -  筛选/排序：操作按钮为水墨印章样式，点击后有印章盖下的反馈动画，筛选结果以水墨淡入方式更新。
    -  详情页：打开时从右往左/从上往下以卷轴展开动画呈现，关闭/返回时反向收起，页面内信息加载带有轻微水墨晕染渐变效果。

请提供完整的项目架构目录、核心文件代码（包括App.vue、main.ts、Pinia仓库、成员卡片组件、详情页组件、路由配置），并附带详细的代码注释和运行说明。


1. 帮会成员数据接口 + 模拟数据生成提示词
请为Vue3 + TypeScript 水墨武侠帮会网站，定义**类型安全**的帮会成员数据接口（Interface），并模拟60条符合要求的成员数据，要求如下：
1.  接口名称：GangMember，包含以下字段（均需定义明确类型，避免any）：
    -  唯一标识：id（number）
    -  成员昵称：nickname（string，古风名称，如「墨影刀客」「竹间居士」）
    -  帮会职位：position（string，可选值：帮主、副帮主、长老、堂主、普通帮众）
    -  职业类型：profession（string，可选值：神相、碎梦、铁衣、血河、素问、九灵、龙吟、玄机，必须严格匹配）
    -  战力值：combatPower（number，范围1000-99999）
    -  所属门派：faction（string，如武当、丐帮、明教、峨眉、唐门）
    -  水墨头像地址：avatar（string，可填模拟图片地址或空字符串）
    -  武侠风简介：intro（string，1-2句，贴合职业特性，如碎梦：「一柄寒刀破万军，踏影而行无踪迹」）
    -  入帮时间：joinTime（string，格式「YYYY-MM-DD」）
2.  模拟数据：
    -  生成60条GangMember类型数据，职位分布合理（1名帮主、2名副帮主、5名长老、12名堂主、40名普通帮众）。
    -  职业分布均匀（8大职业各5-10条数据），战力值随职位递增，同职位下输出类职业（碎梦、神相、玄机）战力略高于防御/辅助类（铁衣、素问）。
    -  昵称和简介贴合对应职业特性，数据格式符合TS类型规范，可直接导入Pinia仓库使用。
3.  输出：以TS文件格式输出（文件名：gangMemberData.ts），包含Interface定义和export const gangMemberList: GangMember[] 模拟数据。


2. 水墨风格成员卡片组件（带职业展示 + 点击跳转）生成提示词
请基于Vue3 + TypeScript + SCSS 构建一个**水墨武侠风格**的帮会成员卡片组件（GangMemberCard.vue），要求如下：
1.  组件类型：全局通用组件，接收props（类型为GangMember接口），展示成员核心信息，支持点击跳转详情页。
2.  风格设计：
    -  卡片容器：宣纸背景（带有轻微纹理），边框为毛笔笔触样式（黑色，略带晕染效果），卡片形状为圆角矩形或卷轴式（左侧带有卷轴轴头），宽度280px（PC端）。
    -  内容布局：从上到下依次展示「水墨头像（居中，圆形，带有水墨边框）」「成员昵称（古风毛笔字体，居中，深黑色）」「职业（仿宋字体，赭石色，居中，附带简易水墨职业标识小图标可选）」「职位+战力值（灰色，左右分布）」。
    -  职业高亮：针对不同职业可添加轻微专属点缀（如碎梦：淡红色笔触描边、铁衣：深灰色厚重边框），不破坏整体水墨黑白灰主风格。
    -  交互反馈：hover时卡片轻微上浮（transform: translateY(-5px)），边框变为深黑色且水墨晕染效果增强，光标变为毛笔样式，卡片底部浮现「点击查看详情」淡灰色毛笔小字。
3.  技术要求：
    -  使用Setup语法糖，定义严格的Props类型（基于GangMember接口），避免类型错误，禁用any。
    -  跳转逻辑：通过Vue Router编程式导航，点击卡片整体触发router.push(`/gang/member/${props.member.id}`)，跳转至对应成员详情页。
    -  样式使用SCSS编写，封装水墨风格通用样式，不使用第三方UI库，支持响应式（平板端卡片宽度自适应，最小200px）。
    -  可选优化：添加卡片点击时的轻微水墨爆破/晕染反馈动画（时长0.3s）。
4.  输出：完整的GangMemberCard.vue代码，包含template、script setup（TS）、style scoped（SCSS），附带详细注释，样式可直接运行呈现水墨效果，跳转逻辑完整。

3. 个人信息详情页组件（卷轴式 + 职业详情）生成提示词
请基于Vue3 + TypeScript + SCSS 构建一个**水墨武侠风格**的帮会成员详情页组件（GangMemberDetail.vue），要求如下：
1.  组件类型：路由组件，通过动态路由参数（id）获取对应成员数据，展示完整个人信息。
2.  核心需求：
    -  数据获取：从Pinia仓库中根据路由参数id筛选出对应GangMember数据，类型安全，处理数据不存在的兜底提示（水墨风格「暂无该成员信息」文案）。
    -  职业详情：展示职业名称的同时，附带职业特性描述（如「神相 - 远程输出职业，机动性强」），文案居中，仿宋字体，深灰色。
3.  风格设计：
    -  页面容器：全屏宣纸背景，核心内容区域为「横向卷轴式」（左右带有卷轴轴头，宣纸纹理，毛笔笔触边框），宽度80%（PC端），居中展示。
    -  内容布局：从上到下依次为「返回按钮（水墨印章样式，左侧）」「成员信息标题（帮会成员详情，毛笔字体，居中）」「个人信息卡片（网格布局，2列）」「简介区域（通栏，毛笔字体，带有水墨段落间距）」。
    -  个人信息卡片：左侧展示放大版水墨头像（200px*200px），右侧依次展示昵称、职位、职业（含特性）、战力值、门派、入帮时间，每项信息带有毛笔笔触标签（如「昵称：」「职业：」）。
    -  动画效果：页面加载时触发「从右往左卷轴展开」动画（时长0.8s，舒缓流畅），信息项依次水墨淡入（间隔0.1s）。
4.  技术要求：
    -  使用Setup语法糖，通过useRoute()获取路由参数id，通过useStore()获取Pinia仓库中的成员数据，定义严格的类型推断。
    -  返回逻辑：添加「返回列表」水墨印章按钮，点击触发router.back()，返回上一页，按钮hover时有印章盖下的颜色加深反馈。
    -  样式使用SCSS编写，封装卷轴式布局和水墨详情样式，支持响应式（平板端改为纵向卷轴，头像居中，信息项纵向排列）。
5.  输出：完整的GangMemberDetail.vue代码，包含数据获取、路由参数处理、样式布局、交互逻辑，附带详细注释，可直接作为路由组件挂载使用。
4. 分页 + 双筛选组件（职位 + 职业，水墨风格）生成提示词
请基于Vue3 + TypeScript + SCSS 构建一个**水墨武侠风格**的分页+双筛选组件（GangMemberFilterPagination.vue），用于60人帮会成员列表的筛选和分页，要求如下：
1.  核心功能：
    -  双筛选功能：① 职位筛选（下拉选择框/印章按钮组），选项为「全部」「帮主」「副帮主」「长老」「堂主」「普通帮众」；② 职业筛选（下拉选择框/印章按钮组），选项为「全部」「神相」「碎梦」「铁衣」「血河」「素问」「九灵」「龙吟」「玄机」。
    -  分页功能：显示当前页码、总页数、总人数（格式：「共60名帮众，当前第1/5页」），支持「上一页」「下一页」「跳转到指定页码」操作。
2.  风格设计：
    -  整体风格：与成员卡片、详情页保持一致，宣纸背景、毛笔笔触边框，外层包裹水墨风格容器，间距合理。
    -  筛选组件：采用自定义水墨风格下拉选择框（替换原生样式），下拉列表带有宣纸纹理、水墨分隔线，选中项带有赭石色毛笔笔触高亮；或采用水墨印章式按钮组（每个选项为圆形印章，选中时印章深黑色填充，未选中时浅灰色描边）。
    -  分页按钮：「上一页/下一页」为毛笔书法文字（「上页」「下页」），带有水墨边框，点击时颜色加深；页码按钮为圆形水墨印章，当前页码印章深黑色高亮，其余浅灰色，点击时有印章按压反馈。
3.  技术要求：
    -  使用Setup语法糖，定义Props（总数据量、当前页码、每页条数、当前筛选条件）和Emit（职位筛选变更、职业筛选变更、页码变更），类型严格。
    -  与Pinia联动：接收Pinia中的筛选条件和分页信息，触发变更时通过Emit同步更新Pinia仓库数据，实现筛选/分页结果实时刷新。
    -  样式使用SCSS编写，封装通用水墨按钮/选择框样式，响应式适配（平板端筛选组件纵向排列，分页组件居中折叠）。
4.  输出：完整的GangMemberFilterPagination.vue代码，包含样式、交互逻辑、Pinia联动逻辑，附带注释，可直接与成员列表组件配合使用。

三、 视觉样式强化提示词（水墨风格 + 职业优化）

2. 路由切换 + 详情页卷轴动画优化提示词
请为Vue3 + TypeScript 水墨武侠帮会网站，实现**路由切换+详情页卷轴展开**的水墨动画效果，要求如下：
1.  技术方案：基于Vue Router + Vue3 过渡组件（Transition）实现全局路由动画，详情页单独优化卷轴展开效果。
2.  全局路由切换动画：
    -  路由进入时：页面从透明（opacity:0）到不透明（opacity:1），伴随宣纸背景纹理从浅灰（#f8f5f0）逐渐加深为仿宣纸色（#f5f2ed），内容区域从右往左缓慢滑入（transform: translateX(50px) → translateX(0)），带有轻微毛笔笔触拖影效果。
    -  路由离开时：页面从不透明到透明，内容区域缓慢向右淡出（transform: translateX(0) → translateX(50px)），背景宣纸纹理逐渐变淡，动画时长0.8s，节奏舒缓符合古风氛围。
    -  配置方式：在App.vue中配置全局<Transition>包裹<RouterView>，定义对应的CSS过渡类（v-enter、v-enter-active、v-leave、v-leave-active）。
3.  详情页专属卷轴展开动画：
    -  触发时机：详情页路由加载完成后（onMounted）触发，仅在首次进入详情页时执行。
    -  动画效果：核心内容区域（卷轴容器）初始宽度为0，右侧卷轴轴头固定，左侧从右往左逐渐展开（width: 0 → 80%），同时卷轴内的宣纸背景和文字信息依次水墨淡入，动画时长1s，带有轻微的纸张摩擦感（可选CSS transform-origin优化）。
    -  样式要求：使用SCSS编写keyframes动画，结合CSS transition，避免动画卡顿，兼容不同屏幕尺寸，平板端卷轴改为纵向展开（height: 0 → auto）。
4.  技术要求：
    -  使用TypeScript编写，在App.vue中配置全局路由过渡容器，在GangMemberDetail.vue中配置专属卷轴动画。
    -  动画样式使用SCSS封装，可复用，不影响页面正常功能和响应式适配，避免与其他组件样式冲突。
    -  兜底处理：添加动画加载失败的默认样式，确保无动画时页面布局正常。
5.  输出：修改后的App.vue代码（含全局路由过渡配置）、GangMemberDetail.vue的卷轴动画样式、对应的SCSS动画类，附带详细注释，可直接运行看到流畅的水墨动画效果。